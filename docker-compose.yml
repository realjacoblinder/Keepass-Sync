services:
  keepass-sync:
    build: .
    container_name: keepass-sync
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
